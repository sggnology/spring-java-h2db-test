spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:/h2-db/test2
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
#    defer-datasource-initialization: true


